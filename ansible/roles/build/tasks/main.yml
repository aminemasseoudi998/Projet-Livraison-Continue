---
# tasks file for build
                  
                 - name: install dep
                   shell: npm install --save --legacy-peer-deps
                 - name: build app
                   shell: ng build
